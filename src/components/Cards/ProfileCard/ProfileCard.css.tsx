import styled from "styled-components";
import { Colors } from "@components/utils/cssMedia";

export const ProfileCardImage = styled.img`
    width: 8rem;
    border-radius: 25rem;
    border: 0.3rem solid ${Colors.coreDarkerBlue};
`;